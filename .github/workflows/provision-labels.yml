name: Provision marketplace labels

on:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  provision:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Create or repair required labels
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          gh label create submission --color 1d76db --description "Plugin marketplace submission" --force
          gh label create plugin-update --color 1d76db --description "Candidate exact-commit plugin update" --force
          gh label create validated --color 2da44e --description "Automated marketplace checks passed" --force
          gh label create needs-fixes --color d1242f --description "Submission needs changes before review" --force
          gh label create security-needs-fixes --color b60205 --description "Automated security baseline found a pattern requiring fixes" --force
          gh label create security-review-required --color fbca04 --description "Security evidence requires maintainer attention" --force
          gh label create maintainer-verified --color 2da44e --description "Accept review-required capabilities for exact-commit verification" --force
          gh label create standard-installation-approved --color 2da44e --description "Approve a passing exact-snapshot standard-installation change" --force
          gh label create approved-and-verified --color 2da44e --description "Approve and verify an exact marketplace snapshot" --force
          gh label create approved-for-listing --color bf4b55 --description "Legacy listing approval; no longer publishes new submissions" --force
          gh label create manual-setup --color fbca04 --description "Standard install cannot produce a functioning plugin" --force
          gh label create listed --color 8250df --description "Published in the marketplace" --force
